services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: approximpc:dev
    container_name: approximpc-dev
    working_dir: /app
    stdin_open: true
    tty: true
    environment:
      DISPLAY: ${DISPLAY:-:0}
      QT_X11_NO_MITSHM: "1"
      ACADOS_SOURCE_DIR: /opt/acados
      LD_LIBRARY_PATH: /opt/acados/lib:${LD_LIBRARY_PATH:-}
    volumes:
      # Single bind mount for the repo (source of truth for mounts).
      - .:/app
      # Named volume for persistent environment across rebuilds.
      - venv:/app/.venv
      # X11 socket for GUI rendering.
      - /tmp/.X11-unix:/tmp/.X11-unix:rw
    command: bash

  # GPU-enabled variant (opt-in via --profile gpu).
  app-gpu:
    extends:
      service: app
    container_name: approximpc-dev-gpu
    profiles: ["gpu"]
    gpus: all
    environment:
      NVIDIA_VISIBLE_DEVICES: all
      NVIDIA_DRIVER_CAPABILITIES: compute,utility,graphics

volumes:
  venv:
