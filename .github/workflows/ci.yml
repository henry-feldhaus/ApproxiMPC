name: CI

on:
  push:
    branches: [ main , 'v*', 'dev*']
  pull_request:
    branches: [ main , 'v*' ]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12"]
    # env:
    #   QT_QPA_PLATFORM: offscreen

    steps:
    - name: Update apt
      run: sudo apt-get update

    - name: Install openGL
      run: sudo apt-get install -y freeglut3-dev libglib2.0-0 libsm6 libxrender1 libxext6 libxkbcommon-x11-0 libdbus-1-dev

    - uses: actions/checkout@v2
      with:
        submodules: recursive

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v2
      with:
        python-version: ${{ matrix.python-version }}
        
    - name: Install
      run: |
        python -m pip install --upgrade pip
        pip install poetry
        poetry install --all-groups
    
    - name: Test with pytest
      run: |
        poetry run pytest

